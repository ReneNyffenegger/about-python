needles = np.array([
       np.array([
          np.random.normal(0.5, 0.01),
          np.random.normal(0.5, 0.01),
          np.random.normal(0.5, 0.01)
     ])
     for _ in range(10000)
   ]).astype(np.float32)
